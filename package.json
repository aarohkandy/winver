{
  "name": "winver",
  "version": "0.1.0",
  "private": false,
  "description": "Mac-to-Windows worker helper for a spare Surface.",
  "bin": {
    "winver": "./bin/winver.js"
  },
  "scripts": {
    "test": "node --test",
    "syntax": "node --check bin/winver.js && node --check lib/cli.js && node --check lib/config.js && node --check lib/remote.js && node --check lib/ui.js && node --check scripts/secret-scan.js",
    "secret-scan": "node scripts/secret-scan.js",
    "check": "npm run syntax && npm run secret-scan && npm test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
